= simple_form_for [Subject.find(params[:subject_id]), @manual], html: {class: "form-horizontal"} do |f|
	= f.input :name, label: "Название"
	= f.input :file, label: "Файл"
	.form-actions
		= f.button :submit, 'Сохранить', class: "btn btn-inverse"
