%ul.breadcrumb
	%li
		%i.icon-chevron-right
		&nbsp;
	%li 
		Студентам
	%li
		%i.icon-chevron-right
		&nbsp;
	%li 
		%a{ href: schedules_path }
			Расписание занятий
			= @subtitle
	%li
		%i.icon-chevron-right
		&nbsp;
	%li
		%a.active{ href: schedule_path(@schedule.id) }= @schedule.name

%div#page{ :style=>"display:none" }show_schedule


.page-header
	%h2
		Расписание занятий
		%small= @schedule.name
		%a.btn.btn-danger#edit-schedule Редактировать

.span8.offset2.alert.alert-success{ :style=>"display:none" }
	%a.close#close{"data-dissmiss"=>"alert" } &times;
	%h4 Успешно сохранено
	= "Расписание #{@schedule.name} было изменено."

%br

= @schedule.body.html_safe

%script{ :language => :javascript}
	= "var sheduleID = #{@schedule.id};"

